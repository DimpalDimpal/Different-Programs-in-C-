#include<stdio.h>

int main()
{
	int a=10;
	int *b;  //pointer variable is denoted as *NAME
	printf("value of a=%d, address of a=%x",a,&a);
	printf("the address location stored by b is %x",b);
	return 0;
}
